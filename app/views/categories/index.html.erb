<div class="page-header">
  <h1>Categories</h1>
</div>

<% @categories.each do |category| %>
  <h4><%= category.title %></h4>
  <p><%= category.description %></p>
  <p><%= link_to "View Loan Requests", category_path(category), class: "category" %></p>
<% end %>
